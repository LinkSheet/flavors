<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="fe.linksheet.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.nightly.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.debug.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.pro.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.pro.nightly.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.pro.debug.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.foss.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.foss.nightly.permission.INTERCONNECT" />
    <uses-permission android:name="fe.linksheet.foss.debug.permission.INTERCONNECT" />

    <queries>
        <!-- Legacy LinkSheet packages -->
        <package android:name="fe.linksheet" />
        <package android:name="fe.linksheet.nightly" />
        <package android:name="fe.linksheet.debug" />

        <!-- LinkSheet Pro packages -->
        <package android:name="fe.linksheet.pro" />
        <package android:name="fe.linksheet.pro.nightly" />
        <package android:name="fe.linksheet.pro.debug" />

        <!-- LinkSheet FOSS packages -->
        <package android:name="fe.linksheet.foss" />
        <package android:name="fe.linksheet.foss.nightly" />
        <package android:name="fe.linksheet.foss.debug" />
    </queries>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.TestApp">
        <activity
                android:name="app.linksheet.testapp.interconnect.MainActivity"
                android:exported="true"
                android:theme="@style/Theme.TestApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:scheme="http" />

                <data android:host="linksheet.app" />
            </intent-filter>
        </activity>
    </application>

</manifest>
